from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='MLOps project to estimate crime types using mlops',
    author='niconomist98',
    license='MIT',
)
